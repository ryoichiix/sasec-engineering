VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Anthropic key for the Drawing Sheet Weight Calculator (Claude vision table extraction).
# NOTE: this is called directly from the browser, so the key is bundled into the
# client and is effectively semi-public — scope/rotate it accordingly.
VITE_ANTHROPIC_API_KEY=sk-ant-...
